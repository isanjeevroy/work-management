<nav class="bg-white border-b shadow-sm sticky top-0 z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      
      <!-- Left links -->
      <div class="flex space-x-6">
        <a href="/data" class="text-gray-800 font-semibold hover:text-blue-600 transition-colors">Home</a>
        <a href="/newjob" class="text-gray-800 font-semibold hover:text-blue-600 transition-colors">Add Job</a>
      </div>

      <!-- Right (Logout) -->
      <% if (currUser) { %>
        <a href="/logout">
          <button class="bg-red-600 text-white px-4 py-2 rounded-lg font-medium shadow hover:bg-red-700 transition-colors">
            Logout
          </button>
        </a>
      <% } %>

    </div>
  </div>
</nav>
