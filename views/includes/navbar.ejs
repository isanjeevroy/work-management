<nav class="bg-white/80 backdrop-blur-md shadow-sm sticky top-0 z-50">
  <div class="container mx-auto px-6 py-4">
    <div class="flex justify-between items-center">
      <!-- Logo -->

      <% if(!currUser){ %>
        <a href="/">

          <% } %>

            <% if(currUser){ %>
              <a href="/dashboard">
                <% } %>
                  <div class="flex items-center space-x-2">
                    <i class="fas fa-tasks text-3xl text-indigo-600"></i>
                    <span class="text-2xl font-bold gradient-text">WorkFlow Pro</span>
                  </div>
              </a>

              <!-- Desktop Navigation -->
              <% if(!currUser){ %>
                <div class="hidden md:flex space-x-8">
                  <a href="#features" class="text-gray-700 hover:text-indigo-600 transition-colors">Features</a>
                  <a href="#how-it-works" class="text-gray-700 hover:text-indigo-600 transition-colors">How It Works</a>
                  <a href="#benefits" class="text-gray-700 hover:text-indigo-600 transition-colors">Benefits</a>
                </div>
                <% } %>

                  <!-- Desktop Buttons -->
                  <div class="hidden md:flex space-x-4">

                    <% if(!currUser){ %>
                      <a href="/login">
                        <button
                          class="px-4 py-2 text-indigo-600 border border-indigo-600 rounded-lg hover:bg-indigo-50 transition-colors cursor-pointer">Sign
                          In</button>
                      </a>

                      <a href="/signup">
                        <button
                          class="px-4 py-2 bg-gradient-to-r from-indigo-600 to-purple-600 text-white rounded-lg hover:shadow-lg transition-all transform hover:scale-105 cursor-pointer">Get
                          Started</button>
                      </a>

                      <% } %>

                        <% if(currUser){ %>
                          <a href="/logout">
                            <button
                              class="px-4 py-2 bg-gradient-to-r from-red-200 to-red-600 text-white rounded-lg hover:shadow-lg transition-all transform hover:scale-105">
                              Logout </button>
                          </a>
                          <% } %>
                  </div>

                  <!-- Mobile Hamburger Button -->
                  <button class="md:hidden hamburger flex flex-col justify-center items-center w-8 h-8 space-y-1"
                    id="mobile-menu-btn">
                    <span class="hamburger-line w-6 h-0.5 bg-gray-700"></span>
                    <span class="hamburger-line w-6 h-0.5 bg-gray-700"></span>
                    <span class="hamburger-line w-6 h-0.5 bg-gray-700"></span>
                  </button>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div class="md:hidden mobile-menu fixed inset-0 top-[72px] bg-white/95 backdrop-blur-md z-40" id="mobile-menu">
    <div class="flex flex-col p-6 space-y-6 bg-amber-300">
      <!-- Mobile Navigation Links -->

      <% if(!currUser){ %>
        <a href="#features"
          class="text-gray-700 hover:text-indigo-600 transition-colors text-lg py-2 border-b border-gray-100">Features</a>
        <a href="#how-it-works"
          class="text-gray-700 hover:text-indigo-600 transition-colors text-lg py-2 border-b border-gray-100">How It
          Works</a>
        <a href="#benefits"
          class="text-gray-700 hover:text-indigo-600 transition-colors text-lg py-2 border-b border-gray-100">Benefits</a>
        <% } %>


          <!-- Mobile Buttons -->
          <div class="flex flex-col space-y-4 mt-8">
            <% if(!currUser){ %>
              <a href="/login">
                <button
                  class="w-full px-4 py-3 text-indigo-600 border border-indigo-600 rounded-lg hover:bg-indigo-50 transition-colors">Sign
                  In</button>
              </a>

              <a href="/signup">
                <button
                  class="w-full px-4 py-3 bg-gradient-to-r from-indigo-600 to-purple-600 text-white rounded-lg hover:shadow-lg transition-all">Get
                  Started</button>
              </a>
              <% } %>

                <% if(currUser){ %>
                  <a href="/logout">
                    <button
                      class="w-full px-4 py-3 bg-gradient-to-r from-indigo-600 to-purple-600 text-white rounded-lg hover:shadow-lg transition-all">
                      Logout
                    </button>
                  </a>
                  <% } %>
          </div>
    </div>
  </div>
</nav>